import wallet from './wallet';
import walletDetail from './walletDetail';
import order from './order';
import orderDetail from './orderDetail';

const payReducer = { wallet, walletDetail, order, orderDetail }

export default payReducer;